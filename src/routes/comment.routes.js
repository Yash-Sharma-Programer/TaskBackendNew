import { Router } from 'express'; import * as controller from '../controllers/comment.controller.js'; import { requireAuth, requireOrganisation } from '../middleware/auth.js'; import { validate } from '../middleware/validate.js'; import { commentSchema } from '../validators/domain.validators.js';
const router = Router(); router.use(requireAuth, requireOrganisation); router.get('/', controller.list); router.post('/', validate(commentSchema), controller.create); router.patch('/:id', controller.update); router.post('/:id/reactions', controller.react); router.delete('/:id', controller.remove); export default router;
