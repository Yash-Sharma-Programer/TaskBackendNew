import { Router } from 'express'; import * as controller from '../controllers/notification.controller.js'; import { requireAuth, requireOrganisation } from '../middleware/auth.js'; const router = Router(); router.use(requireAuth, requireOrganisation); router.get('/', controller.list); router.patch('/read-all', controller.readAll); router.patch('/:id/read', controller.read); router.delete('/:id', controller.remove); export default router;
