import { Router } from 'express'; import * as controller from '../controllers/file.controller.js'; import { requireAuth, requireOrganisation } from '../middleware/auth.js'; import { upload } from '../middleware/upload.js'; const router = Router(); router.use(requireAuth, requireOrganisation); router.get('/', controller.list); router.post('/', upload.array('files', 6), controller.upload); router.delete('/:id', controller.remove); export default router;
