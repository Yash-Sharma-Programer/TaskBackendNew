import { Task, Notification } from '../models/index.js';
export const createDeadlineNotifications = async () => { const from = new Date(), to = new Date(Date.now() + 24 * 60 * 60 * 1000); const tasks = await Task.find({ deadline: { $gte: from, $lte: to }, completedAt: null }); for (const task of tasks) for (const userId of task.assignees) { const exists = await Notification.exists({ userId, entityId: task._id, type: 'deadline', createdAt: { $gte: new Date(Date.now() - 20 * 60 * 60 * 1000) } }); if (!exists) await Notification.create({ organisationId: task.organisationId, userId, type: 'deadline', title: 'Task due soon', message: task.title, entityType: 'task', entityId: task._id, link: `/projects/${task.projectId}/board?task=${task._id}` }); } };
